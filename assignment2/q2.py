array = []

arr_elements = int(input("Enter the number of elements to be added: "))

for i in range(arr_elements):
    element = input("Enter element ")
    array.append(element)

print("Final array:", array)